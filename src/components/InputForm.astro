---
const { label, type, placeholder, id } = Astro.props
---
<div class="flex flex-col mb-10">
    <label for={id} class="text-center text-slate-700 text-sm">{label}</label>
    <input id={id} type={type} placeholder={placeholder} class="px-5 py-3 bg-transparent outline-none text-slate-700 text-center border-b border-b-slate-600 md:w-[50vw]" autocomplete="off">
</div>