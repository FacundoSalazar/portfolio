---
const { title, desc, imgURL } = Astro.props;
---

<article class="flex flex-col justify-between h-60 w-60 rounded-lg shadow-md">
    <img src={imgURL} alt="" class="h-[50%] rounded-t">
    <h3 class="text-base text-center px-2 text-balance uppercase font-bold">{title}</h3>
    <p class="text-center text-balance text-sm">{desc}</p>
</article>

<style>
    .text-balance {
		text-wrap: balance;
	}
</style>