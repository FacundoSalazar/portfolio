---
const { title, desc, imgURL, url } = Astro.props;
---

<article class="flex flex-col justify-between h-80 w-80 rounded-lg shadow-md hover:shadow-xl duration-300">
    <img src={imgURL} alt={`${title} project.`} class="h-[50%] rounded-t" width="2798px" height="1514px">
    <h3 class="text-slate-600 text-xs text-center px-2 text-balance uppercase font-bold">{title}</h3>
    <p class="text-slate-500 text-center text-balance text-xs p-3 rounded-b">{desc}</p>
    <a href={url} class="w-[50%] text-center self-center rounded bg-slate-700 text-white hover:bg-slate-600 duration-300 active:opacity-75">VIEW CODE</a>
</article>

